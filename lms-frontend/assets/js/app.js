/* ============================================================
   app.js  |  Main Entry Point
   Bootstraps the IJED LMS after DOM is ready.
   ============================================================ */

"use strict";

document.addEventListener('DOMContentLoaded', () => {
  App.init();
});
